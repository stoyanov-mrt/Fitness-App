# Copy to .env.local (git-ignored) once a Supabase project exists.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# Optional — crash reporting is a safe no-op until this is set (see
# src/lib/sentry.ts). Get one from sentry.io once a project exists there.
EXPO_PUBLIC_SENTRY_DSN=
