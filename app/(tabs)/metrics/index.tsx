import { ScreenPlaceholder } from "@/components/ScreenPlaceholder";

export default function MetricsScreen() {
  return (
    <ScreenPlaceholder
      title="Body Metrics"
      description="Weight, measurements, and progress photos are built in Phase 4."
    />
  );
}
